import React from 'react';
import { Typography } from '@mui/material';

export default function ImageNumber() {
    return (
        <Typography variant="caption" display="block" align="center" color="textSecondary" sx={{ fontSize: '8px' }}>
          16 images per page
        </Typography>
      );
}